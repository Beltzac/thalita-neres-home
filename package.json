{
  "name": "thalita-neres-interactive-menus",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "precompute:centers": "node scripts/precompute-centers.js",
    "build": "npm run precompute:centers && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "sharp": "^0.33.5"
  }
}
